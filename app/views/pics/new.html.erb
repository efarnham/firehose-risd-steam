 <div class="streamy-hottub">
	<h2>
	I love hottub coding
 
	<%= simple_form_for @pic do |f| %>
		<%= f.input :hour %>
		<%= f.input :emotion %>
		<%= f.input :learned  %>

		<%= f.submit 'Save' %>
	<% end %>

	</h2>
</div>